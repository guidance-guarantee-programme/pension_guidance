<%= render layout: 'layouts/base' do %>
  <div class="l-masthead">
    <div class="l-banner-container">
      <%= render 'components/logo_link' %>
      <%= render 'components/page_nav' %>
    </div>
  </div>
  <%= render 'components/language_banner' if show_language_banner? %>
  <main role="main" id="content" tabindex="-1">
    <%= render 'components/breadcrumbs' %>

    <div class="l-grid-row">
      <div class="l-column-two-thirds">
        <%= yield %>
      </div>
      <div class="l-column-third l-column-third--sidebar">
        <% if book_an_appointment_link? %>
          <% guide_path_options = defined?(@guide) ? { icn: 'book-appointment', ici: @guide.ga_origin } : {} %>
          <%= link_to guide_path('appointments', guide_path_options), class: 'book-appointment' do %>
            <%= t('layout.book_a_free_appointment') %>
          <% end %>
        <% end %>

        <%= yield :sidebar %>
        <%= yield :sticky_sidebar %>
      </div>
    </div>
  </main>
<% end %>
