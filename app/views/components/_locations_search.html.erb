<% has_error = local_assigns[:has_error] %>
<form action="/locations" method="get">
  <div class="form-group <%= 'error' if has_error %>">
    <label class="form-label-bold" for="location">
      Postcode
      <span class="form-hint">For example, 'SW1A 1AA'</span>
      <% if has_error %>
        <%= yield %>
      <% end %>
    </label>
    <input type="text" class="form-control" name="postcode" id="location" value="<%= @postcode&.squish %>">
    <input type="submit" class="button" value="Search">
  </div>
</form>
