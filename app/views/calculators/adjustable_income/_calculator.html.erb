<%= render 'form', form: form %>
<% if form.valid? %>
  <%= render 'estimate',
    estimate: form.estimate,
    pot: form.pot,
    monthly_drawdown_amount: form.estimate[:monthly_drawdown_amount]
  %>
<% end %>
